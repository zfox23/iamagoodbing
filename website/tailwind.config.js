const defaultTheme = require('tailwindcss/defaultTheme');
const { rootCertificates } = require('tls');

module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
  ],
  darkMode: "class"
}
